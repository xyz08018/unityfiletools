public class Global : MonoSingleton<Global>
{
    public bool IsDebugPostUrl { get; private set; }
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
